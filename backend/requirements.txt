# 공용 가상환경(../../.venv, Roadmap-Agent 와 함께 씀)에 설치된 실제 버전으로 고정.
fastapi==0.141.1
uvicorn[standard]==0.52.2
pydantic==2.13.4

# Local development: this project shares one virtual environment with Roadmap-Agent
# (see the "SeedUp 백엔드·프론트엔드 실행" section in ../Roadmap-Agent/README.md).
# Install the adjacent repository into that shared venv with:
#   pip install -e ../Roadmap-Agent
# Deployment: Roadmap-Agent is baked into its own Docker image and used as the base
# image for this backend's Dockerfile — see SeedUp/backend/Dockerfile.
